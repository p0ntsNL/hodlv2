name: PR
on:
  pull_request:

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: stefanwarmerdam/python-lint@v1.5.0
        with:
          use-pylint: true
          use-black: true
          use-isort: true
          use-pycodestyle: false
          use-flake8: false
          use-mypy: false
